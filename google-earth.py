#!/usr/bin/python3
import webbrowser

url = "https://earth.google.com/web/"

# Choose a specific browser - uncomment the one you want
# webbrowser.get('firefox').open(url)
# webbrowser.get('google-chrome').open(url)
# webbrowser.get('chromium').open(url)

# Or just use the default browser
webbrowser.open(url)
